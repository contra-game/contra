# Godot
.godot/
/android/
export_presets.cfg

# Исходники скачанных ассетов: в игре используются уже распакованные
# и обрезанные файлы в assets/city и assets/sfx
assets/downloads/
assets/raw/

# Кадры, которые делает tools/screenshot.tscn
tools/*.png
tools/*.png.import

# App ID Photon: свой у каждого, в публичный репозиторий не кладётся
photon.cfg

# Photon Fusion SDK: проприетарный, распространять нельзя — ставится вручную
addons/fusion/
